# =========================================
# 繁體中文程式語言 — 範例程式
# 檔案：範例.中文
# =========================================

# 1. 變數宣告與基本算術
令 甲 為 整數 等於 10。
令 乙 為 整數 等於 4。
令 總和 為 整數 等於 甲 加 乙。
令 差 為 整數 等於 甲 減 乙。
令 積 為 整數 等於 甲 乘 乙。
令 商 為 整數 等於 甲 除 乙。

顯示 「=== 算術運算 ===」。
顯示 總和。
顯示 差。
顯示 積。
顯示 商。

# 2. 字串串接
令 問候 為 字串 等於 「你好，」。
令 名字 為 字串 等於 「世界！」。
令 全句 為 字串 等於 問候 加 名字。
顯示 「=== 字串 ===」。
顯示 全句。

# 3. 條件判斷
顯示 「=== 條件判斷 ===」。
令 分數 為 整數 等於 72。
如果（分數 大於等於 90）則：
  顯示 「優秀」。
。完否則：
  如果（分數 大於等於 60）則：
    顯示 「及格」。
  。完否則：
    顯示 「不及格」。
  。完
。完

# 4. 循環
顯示 「=== 1 到 5 累加 ===」。
令 累計 為 整數 等於 0。
令 索引 為 整數 等於 1。
當（索引 小於等於 5）就：
  令 累計 等於 累計 加 索引。
  令 索引 等於 索引 加 1。
。完
顯示 累計。

# 5. FizzBuzz
顯示 「=== FizzBuzz（1~15）===」。
令 數 為 整數 等於 1。
當（數 小於等於 15）就：
  如果（數 除 15 乘 15 等於 數）則：
    顯示 「FizzBuzz」。
  。完否則：
    如果（數 除 3 乘 3 等於 數）則：
      顯示 「Fizz」。
    。完否則：
      如果（數 除 5 乘 5 等於 數）則：
        顯示 「Buzz」。
      。完否則：
        顯示 數。
      。完
    。完
  。完
  令 數 等於 數 加 1。
。完
