# =============================================
# 繁體中文程式語言 — 氣泡排序（Bubble Sort）
# =============================================
#
# 演算法說明：
#   氣泡排序每輪從頭開始，比較相鄰兩個元素，
#   若左邊大於右邊就交換，最大值會像氣泡一樣
#   浮到末端。重複 N-1 輪後完成排序。
#
# 時間複雜度：O(N²)
# 空間複雜度：O(1)（原地排序）
#
# 語法重點示範：
#   - 整數列表宣告與顯示
#   - 索引存取 資料［索引］
#   - 索引賦值 令 資料［索引］ 等於 值
#   - 巢狀 當...就 循環
#   - 暫存變數實現元素交換
# =============================================

# ── 待排序資料 ──────────────────────────────
令 資料 為 整數列表 等於 ［64，34，25，12，22，11，90］。
令 長度 為 整數 等於 7。

顯示 「=============================」。
顯示 「   氣泡排序 Bubble Sort」。
顯示 「=============================」。
顯示 「排序前：」。
顯示 資料。
顯示 「-----------------------------」。

# ── 外層迴圈：共執行 長度-1 輪 ──────────────
令 外層 為 整數 等於 0。
當（外層 小於等於 長度 減 2）就：

  # ── 內層迴圈：每輪比較到尚未排序的最後一個 ──
  令 內層 為 整數 等於 0。
  當（內層 小於等於 長度 減 外層 減 2）就：

    # 若左邊元素 > 右邊元素，則交換
    如果（資料［內層］ 大於 資料［內層 加 1］）則：
      令 暫存 為 整數 等於 資料［內層］。
      令 資料［內層］ 等於 資料［內層 加 1］。
      令 資料［內層 加 1］ 等於 暫存。
    。完

    令 內層 等於 內層 加 1。
  。完

  # 顯示每輪結束後的狀態
  顯示 資料。
  令 外層 等於 外層 加 1。
。完

顯示 「-----------------------------」。
顯示 「排序後：」。
顯示 資料。
顯示 「=============================」。
