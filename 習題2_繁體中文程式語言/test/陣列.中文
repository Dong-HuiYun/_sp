# =========================================
# 繁體中文程式語言 — 範例程式（含列表）
# =========================================

# 1. 整數列表宣告與顯示
令 數組 為 整數列表 等於 ［10，20，30，40，50］。
顯示 「=== 整數列表 ===」。
顯示 數組。

# 2. 存取特定索引
顯示 「第0個：」。
顯示 數組［0］。
顯示 「第4個：」。
顯示 數組［4］。

# 3. 索引賦值
令 數組［2］ 等於 999。
顯示 「修改後：」。
顯示 數組。

# 4. 走訪列表累加
令 成績 為 整數列表 等於 ［85，92，78，96，88］。
令 總分 為 整數 等於 0。
令 索引 為 整數 等於 0。
當（索引 小於等於 4）就：
  令 總分 等於 總分 加 成績［索引］。
  令 索引 等於 索引 加 1。
。完
顯示 「=== 成績總分 ===」。
顯示 總分。

# 5. 字串列表
令 語言 為 字串列表 等於 ［「繁體中文」，「English」，「日本語」］。
顯示 「=== 字串列表 ===」。
顯示 語言［0］。
令 語言［1］ 等於 「한국어」。
顯示 語言［1］。
