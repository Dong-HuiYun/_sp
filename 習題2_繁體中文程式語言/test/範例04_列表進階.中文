# =====================================================
# 範例 04：列表進階操作
# 測試功能：長度、添加、彈出、動態建構
# =====================================================

顯示 「=== 範例04：列表進階操作 ===」。

# ── 長度 ─────────────────────────────────────────
顯示 「-- 長度 --」。
令 甲表 為 整數列表 等於 ［5，10，15，20，25］。
顯示 長度（甲表）。          # 5

令 乙表 為 字串列表 等於 ［「蘋果」，「香蕉」，「橘子」］。
顯示 長度（乙表）。          # 3

# ── 動態添加 ─────────────────────────────────────
顯示 「-- 動態添加 --」。
令 數列 為 整數列表 等於 ［］。
從 1 到 5 以 索引 做：
  添加 索引 乘 索引 於 數列。
。完
顯示 數列。                  # ［1，4，9，16，25］
顯示 「長度：」。
顯示 長度（數列）。          # 5

# ── 彈出與存入 ───────────────────────────────────
顯示 「-- 彈出 --」。
令 棧 為 整數列表 等於 ［10，20，30，40，50］。
顯示 「初始：」。
顯示 棧。
彈出 棧 並存入 末項。
顯示 「彈出：」。
顯示 末項。                  # 50
彈出 棧 並存入 末項。
顯示 末項。                  # 40
顯示 「剩餘：」。
顯示 棧。                    # ［10，20，30］

# ── 用列表當佇列（先添加後彈出）────────────────
顯示 「-- 用列表模擬堆疊計算 --」。
令 堆疊 為 整數列表 等於 ［］。
添加 3 於 堆疊。
添加 7 於 堆疊。
添加 2 於 堆疊。
彈出 堆疊 並存入 甲。
彈出 堆疊 並存入 乙。
令 和 為 整數 等於 甲 加 乙。
顯示 「7 + 2 = 」。
顯示 和。                    # 9

# ── 走訪 + 累加 ──────────────────────────────────
顯示 「-- 走訪累加 --」。
令 成績 為 整數列表 等於 ［78，92，85，96，67，88］。
令 總分 為 整數 等於 0。
令 人數 為 整數 等於 長度（成績）。
從 0 到 人數 減 1 以 索引 做：
  令 總分 等於 總分 加 成績［索引］。
。完
顯示 「總分：」。
顯示 總分。                  # 506

# ── 反轉列表 ─────────────────────────────────────
顯示 「-- 用彈出反轉列表 --」。
令 原表 為 整數列表 等於 ［1，2，3，4，5］。
令 反表 為 整數列表 等於 ［］。
令 剩餘 為 整數 等於 長度（原表）。
當（剩餘 大於 0）就：
  彈出 原表 並存入 末項。
  添加 末項 於 反表。
  令 剩餘 等於 剩餘 減 1。
。完
顯示 反表。                  # ［5，4，3，2，1］
