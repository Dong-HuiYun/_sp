# =====================================================
# 範例 05：錯誤處理（嘗試/若出錯）
# 測試功能：嘗試、若出錯、各種錯誤類型
# =====================================================

顯示 「=== 範例05：錯誤處理 ===」。

# ── 索引越界 ─────────────────────────────────────
顯示 「-- 攔截索引越界 --」。
嘗試：
  令 表 為 整數列表 等於 ［1，2，3］。
  顯示 表［99］。
。完若出錯：
  顯示 「錯誤：索引 99 超出範圍！」。
。完
顯示 「程式繼續執行」。

# ── 除以零 ───────────────────────────────────────
顯示 「-- 攔截除以零 --」。
嘗試：
  令 除數 為 整數 等於 0。
  令 結果 為 整數 等於 100 除 除數。
  顯示 結果。
。完若出錯：
  顯示 「錯誤：不能除以零！」。
。完
顯示 「程式繼續執行」。

# ── 空列表彈出 ───────────────────────────────────
顯示 「-- 攔截空列表彈出 --」。
嘗試：
  令 空表 為 整數列表 等於 ［］。
  彈出 空表 並存入 值。
  顯示 值。
。完若出錯：
  顯示 「錯誤：列表是空的，無法彈出！」。
。完

# ── 巢狀嘗試 ─────────────────────────────────────
顯示 「-- 巢狀嘗試/若出錯 --」。
嘗試：
  令 表 為 整數列表 等於 ［10，0，20］。
  嘗試：
    令 甲 為 整數 等於 100 除 表［1］。
    顯示 甲。
  。完若出錯：
    顯示 「內層攔截：除以零」。
  。完
  顯示 「內層出錯後，外層繼續」。
。完若出錯：
  顯示 「外層攔截」。
。完

# ── 在迴圈中用嘗試 ───────────────────────────────
顯示 「-- 迴圈中攔截錯誤 --」。
令 除數表 為 整數列表 等於 ［5，2，0，4，0，1］。
令 索引 為 整數 等於 0。
當（索引 小於等於 5）就：
  嘗試：
    令 結果 為 整數 等於 100 除 除數表［索引］。
    顯示 結果。
  。完若出錯：
    顯示 「某個除數是零，跳過」。
  。完
  令 索引 等於 索引 加 1。
。完
