# =====================================================
# 範例 03：函數定義與呼叫（含遞迴）
# 測試功能：定義、回傳、遞迴、多參數
# =====================================================

顯示 「=== 範例03：函數定義與呼叫 ===」。

# ── 無參數函數 ───────────────────────────────────
定義 說哈囉（名字）為：
  回傳 「哈囉，」 加 名字 加 「！」。
。完

顯示 說哈囉（「小明」）。
顯示 說哈囉（「世界」）。

# ── 兩個參數的函數 ───────────────────────────────
定義 最大值（甲，乙）為：
  如果（甲 大於 乙）則：
    回傳 甲。
  。完否則：
    回傳 乙。
  。完
。完

顯示 「-- 最大值 --」。
顯示 最大值（10，25）。     # 25
顯示 最大值（99，7）。      # 99

# ── 遞迴：階乘 ───────────────────────────────────
定義 階乘（數）為：
  如果（數 小於等於 1）則：
    回傳 1。
  。完否則：
    回傳 數 乘 階乘（數 減 1）。
  。完
。完

顯示 「-- 階乘（1~7）--」。
從 1 到 7 以 數 做：
  顯示 階乘（數）。
。完

# ── 遞迴：費氏數列 ───────────────────────────────
定義 費氏（數）為：
  如果（數 小於等於 1）則：
    回傳 數。
  。完否則：
    回傳 費氏（數 減 1）加 費氏（數 減 2）。
  。完
。完

顯示 「-- 費氏數列前10項 --」。
從 0 到 9 以 索引 做：
  顯示 費氏（索引）。
。完

# ── 函數呼叫另一個函數 ───────────────────────────
定義 冪次加一（底，指）為：
  回傳 底 次方 指 加 1。
。完

顯示 「-- 2^5 + 1 = --」。
顯示 冪次加一（2，5）。     # 33
