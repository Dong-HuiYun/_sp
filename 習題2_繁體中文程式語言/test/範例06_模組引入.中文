# =====================================================
# 範例 06：模組引入（引入 數學工具）
# 測試功能：引入、跨檔案函數呼叫
# =====================================================

顯示 「=== 範例06：模組引入 ===」。

引入 數學工具。

# ── 使用模組中的函數 ─────────────────────────────
顯示 「-- 絕對值 --」。
顯示 絕對值（負 7）。        # 7
顯示 絕對值（42）。          # 42
顯示 絕對值（負 100）。      # 100

顯示 「-- 最大值 --」。
顯示 最大值（3，9）。        # 9
顯示 最大值（88，23）。      # 88
顯示 最大值（負 5，負 3）。  # -3（較大）

顯示 「-- 階乘（來自模組）--」。
從 1 到 8 以 數 做：
  顯示 階乘（數）。
。完

# ── 混合使用模組函數與本地邏輯 ───────────────────
顯示 「-- 混合：找1~20中最大的階乘不超過一萬的數 --」。
令 找到 為 整數 等於 0。
從 1 到 20 以 數 做：
  嘗試：
    令 結果 為 整數 等於 階乘（數）。
    如果（結果 小於等於 10000）則：
      令 找到 等於 數。
    。完
  。完若出錯：
    跳出。
  。完
。完
顯示 「答案：」。
顯示 找到。

# ── 在迴圈中使用模組函數 ─────────────────────────
顯示 「-- 取每個數與下一個數的最大值 --」。
令 數列 為 整數列表 等於 ［3，8，1，9，4，7，2，6，5］。
令 大值表 為 整數列表 等於 ［］。
令 索引 為 整數 等於 0。
當（索引 小於等於 長度（數列）減 2）就：
  令 大 為 整數 等於 最大值（數列［索引］，數列［索引 加 1］）。
  添加 大 於 大值表。
  令 索引 等於 索引 加 1。
。完
顯示 大值表。
