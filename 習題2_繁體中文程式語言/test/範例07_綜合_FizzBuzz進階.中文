# =====================================================
# 範例 07：綜合練習 — 進階 FizzBuzz
# 測試功能：餘數、從到、函數、列表、嘗試
# =====================================================

顯示 「=== 範例07：進階FizzBuzz ===」。

# ── 定義 FizzBuzz 判斷函數 ───────────────────────
定義 判斷（數）為：
  如果（數 餘 15 等於 0）則：
    回傳 「FizzBuzz」。
  。完否則：
    如果（數 餘 3 等於 0）則：
      回傳 「Fizz」。
    。完否則：
      如果（數 餘 5 等於 0）則：
        回傳 「Buzz」。
      。完否則：
        回傳 數。
      。完
    。完
  。完
。完

# ── 用函數跑 1~30 ────────────────────────────────
顯示 「-- 1 到 30 的 FizzBuzz --」。
從 1 到 30 以 數 做：
  顯示 判斷（數）。
。完

# ── 收集所有 Fizz 到列表 ─────────────────────────
顯示 「-- 收集1~50中所有Fizz（3的倍數但非5的倍數）--」。
令 三倍數列表 為 整數列表 等於 ［］。
從 1 到 50 以 數 做：
  如果（數 餘 3 等於 0 且 數 餘 5 不等於 0）則：
    添加 數 於 三倍數列表。
  。完
。完
顯示 三倍數列表。
顯示 「共幾個：」。
顯示 長度（三倍數列表）。

# ── 遇到 FizzBuzz 就跳出 ─────────────────────────
顯示 「-- 從1數到第一個FizzBuzz --」。
從 1 到 100 以 數 做：
  如果（數 餘 15 等於 0）則：
    顯示 「第一個FizzBuzz是：」。
    顯示 數。
    跳出。
  。完
。完
