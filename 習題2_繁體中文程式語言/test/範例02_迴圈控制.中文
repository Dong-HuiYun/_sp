# =====================================================
# 範例 02：迴圈控制 — 從到、跳出、繼續
# 測試功能：從...到...以...做、跳出、繼續
# =====================================================

顯示 「=== 範例02：迴圈控制 ===」。

# ── 從到循環基本用法 ─────────────────────────────
顯示 「-- 從1數到5 --」。
從 1 到 5 以 數 做：
  顯示 數。
。完

# ── 跳出：找第一個能整除 7 的數 ─────────────────
顯示 「-- 跳出：找1~50中第一個7的倍數 --」。
從 1 到 50 以 數 做：
  如果（數 餘 7 等於 0）則：
    顯示 「找到了：」。
    顯示 數。
    跳出。
  。完
。完

# ── 繼續：只印偶數 ───────────────────────────────
顯示 「-- 繼續：1到10只顯示偶數 --」。
從 1 到 10 以 數 做：
  如果（數 餘 2 不等於 0）則：
    繼續。
  。完
  顯示 數。
。完

# ── 當迴圈 + 跳出：猜數字模擬 ───────────────────
顯示 「-- 當迴圈+跳出：倒數到遇到3停止 --」。
令 計數 為 整數 等於 10。
當（計數 大於 0）就：
  如果（計數 等於 3）則：
    顯示 「遇到3，停止！」。
    跳出。
  。完
  顯示 計數。
  令 計數 等於 計數 減 1。
。完

# ── 巢狀迴圈 + 繼續：跳過對角線 ────────────────
顯示 「-- 巢狀迴圈：3x3格，跳過對角線 --」。
從 1 到 3 以 列 做：
  從 1 到 3 以 行 做：
    如果（列 等於 行）則：
      繼續。
    。完
    顯示 列 乘 10 加 行。
  。完
。完
