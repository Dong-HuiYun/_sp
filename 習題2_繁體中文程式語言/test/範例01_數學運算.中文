# =====================================================
# 範例 01：數學運算 — 餘數、次方、奇偶判斷
# 測試功能：餘（%）、次方（**）、從到循環
# =====================================================

顯示 「=== 範例01：數學運算 ===」。

# ── 基本餘數 ─────────────────────────────────────
顯示 「-- 餘數運算 --」。
顯示 17 餘 5。        # 2
顯示 100 餘 7。       # 2
顯示 36 餘 6。        # 0

# ── 次方 ─────────────────────────────────────────
顯示 「-- 次方運算 --」。
顯示 2 次方 8。       # 256
顯示 3 次方 4。       # 81
顯示 10 次方 3。      # 1000

# ── 奇偶判斷 ─────────────────────────────────────
顯示 「-- 1到10奇偶判斷 --」。
從 1 到 10 以 數 做：
  如果（數 餘 2 等於 0）則：
    顯示 數 加 0。
  。完否則：
    顯示 數 加 0。
  。完
。完

# ── 九九乘法表（3的倍數那行）────────────────────
顯示 「-- 3的乘法 --」。
從 1 到 9 以 甲 做：
  顯示 3 乘 甲。
。完

# ── 計算 2 的次方表 ───────────────────────────────
顯示 「-- 2的次方表(0~9) --」。
從 0 到 9 以 甲 做：
  顯示 2 次方 甲。
。完
